using Xunit;

namespace Indexed.Abstractions.Tests;

/// <summary>
/// Stage 0 scaffolding sentinel. Proves the solution builds, the xUnit test
/// runner is wired up, and the Abstractions project reference resolves.
/// Replaced by real DTO round-trip tests in Stage 1.
/// </summary>
public sealed class PlaceholderTests
{
    [Fact]
    public void Scaffolding_Builds_And_Runs()
    {
        Assert.True(true);
    }
}
